# ReadTheDocs documentation build requirements
# These are lightweight replacements for heavy dependencies that are mocked during build
# See .readthedocs.yml and docs/source/conf.py for autodoc_mock_imports configuration

sphinx>=7.1.2
sphinx-rtd-theme>=1.3.0
nbsphinx>=0.9.0
pandoc>=2.3

# Lightweight dependencies (not mocked) - required for RST rendering
pandas>=1.5.0
numpy>=1.23.0
